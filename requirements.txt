# Prototype — dépendances minimales, sans extension native à compiler.
fastapi>=0.115
uvicorn>=0.30
sqlalchemy>=2.0
alembic>=1.13
jinja2>=3.1
python-multipart>=0.0.9
pydantic-settings>=2.2
itsdangerous>=2.2
openpyxl>=3.1
tzdata>=2024.1

# Tests
pytest>=8.0
httpx>=0.27

# Pilote PostgreSQL (production / Scalingo). Roues precompilees (aucun compilateur requis).
psycopg[binary]>=3.1

# Optionnel : solveur CP-SAT. Non installable sur Windows ARM64 ; le moteur
# déterministe intégré prend le relais (voir DECISIONS.md D-003).
# ortools>=9.10
